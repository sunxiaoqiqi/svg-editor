{
  "name": "obsidian-svg-canvas-editor",
  "version": "0.1.1",
  "description": "Edit SVG files with source, layers, canvas, and inspector panes.",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/sunxiaoqiqi/svg-editor.git"
  },
  "scripts": {
    "build": "node --check src/main.js && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "lint": "eslint .",
    "check": "node --check src/main.js",
    "release:prepare": "npm run build && node scripts/prepare-release.mjs",
    "release:check": "npm run lint && npm run release:prepare && node scripts/validate-release.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "svg",
    "editor"
  ],
  "author": "suntinglu777",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.0",
    "eslint": "^9.0.0",
    "obsidian": "^1.8.7"
  }
}
